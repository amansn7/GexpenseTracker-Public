services:
  app:
    build: .
    ports:
      - "8000:8000"
    env_file: .env
    volumes:
      - ./data:/app/data
    environment:
      LOCAL_MODE: "true"
      DATABASE_URL: sqlite+aiosqlite:///./data/expense.db
    # No depends_on — SQLite file + in-memory queue, zero external services
